MOVE RH TO POSITION 8
MOVE LH TO POSITION 7
JUMP TO LINE 8 IF LH gt RH # Found value that should be to the right of partition
JUMP TO LINE 8 IF LH eq RH
SHIFT LH TO THE L
JUMP TO LINE 3 IF LHPOS ne 0 # still looking for greater value
STOP
SWAP
MOVE RHPOS TO LHPOS
MOVE LH TO THE R
JUMP TO LINE 8 IF RHCARD gt LHCARD # Needs to keep moving cards right, because partition still has lower values to the right
JUMP TO LINE 3 # Look for More cards just in case a lower value exists
