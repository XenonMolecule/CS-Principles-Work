import sys

file = sys.argv[0]
code = []

with open(path_to_csv) as code_file:
    code.append("LINE NUMBERS AREN'T ZERO INDEXED")
    for i,line in enumerate(code_file):
        code.append(code_file)
